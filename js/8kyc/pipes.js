function pipeFix(numbers){
    let start = numbers[0];
    let finish = numbers[numbers.length-1];
    let ara = [];
    while(start <= finish){  
      ara.push(start);
      start+=1;}
      
    return ara;
    }